export interface PilotaStanding {
  pilotaId: string;
  pilotaName: string;
  teamName: string;
  points: number;
}

export interface TeamStanding {
  teamId: string;
  teamName: string;
  colorHex: string;
  points: number;
}
